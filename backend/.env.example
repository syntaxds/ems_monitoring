PORT=3000
NODE_ENV=production

# Set to true to serve the compiled frontend (/frontend/build) from this server
# as a single-origin deployment. Leave false to host the frontend separately.
SERVE_FRONTEND=false

DB_HOST=localhost
DB_PORT=
DB_NAME=
DB_USER=
DB_PASSWORD=

JWT_SECRET=
JWT_EXPIRES_IN=8h

MQTT_HOST=
MQTT_PORT=
MQTT_USER=
MQTT_PASS=

AI_ENGINE_URL=
AI_API_KEY=
AI_ANOMALY_THRESHOLD=0.75

CORS_ORIGIN=

UPLOAD_DIR=
MAX_IMAGE_SIZE_MB=15

SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM_NAME=
FRONTEND_URL=
